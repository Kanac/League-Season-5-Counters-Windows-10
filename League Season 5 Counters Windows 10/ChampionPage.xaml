<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:League_Season_5_Counters_Windows_10"
      xmlns:data="using:League_of_Legends_Counterpicks.Data"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:triggers="using:WindowsStateTriggers"
      xmlns:Universal="using:Microsoft.AdMediator.Universal" 
      xmlns:UI="using:Microsoft.Advertising.WinRT.UI"
      xmlns:ValueConverter="using:League_of_Legends_Counterpicks.Converters"
      x:Name="pageRoot"
      x:Class="League_Season_5_Counters_Windows_10.ChampionPage"
      mc:Ignorable="d"
      DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Mode=Self}}"
      d:DataContext="{Binding Roles[1].Champions[1], Source={d:DesignData Source=/DataModel/Data.json, Type=data:DataSource}}"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <SlideNavigationTransitionInfo/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="SolidStar" Label="To be used for Live Stats"/>
        </CommandBar>
    </Page.BottomAppBar>
    
    <Page.Resources>
        <ValueConverter:NameToImage x:Key="NameToImageConverter" />
        <ValueConverter:CounterFormat x:Key="CounterConverter" />
        <ValueConverter:PlayingFormat x:Key="PlayingConverter" />
        <ValueConverter:CounterVisiblity x:Key="CounterVisibilityConverter" />
        <ValueConverter:PlayingVisibility x:Key="PlayingVisibilityConverter" />
        <ValueConverter:CounterChampionVisiblity x:Key="CounterChampioniVisibility" />
        <ValueConverter:SynergyChampionVisiblity x:Key="SynergyChampioniVisibility" />
        
        <DataTemplate x:Key="CommentSection">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <StackPanel Margin="0,0,10,0">
                    <Image Name="UpvoteImage"
                           Source="Assets/upvoteblank.png"
                           Height="30"
                           Width="30"
                           DataContextChanged="Upvote_DataLoaded"
                           Tapped="Upvote_Tapped">
                    </Image>
                    <TextBlock Text="{Binding Score}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               FontSize="20">
                    </TextBlock>
                    <Image Name="DownvoteImage"
                           Source="Assets/downvoteblank.png"
                           Height="30"
                           Width="30"
                           DataContextChanged="Downvote_DataLoaded"
                           Tapped="Downvote_Tapped">
                    </Image>
                </StackPanel>

                <Grid Margin="0,0,0,10"
                      Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <TextBlock FontSize="25"
                               Text="{Binding User}">
                    </TextBlock>
                    <TextBlock FontSize="18"
                               Grid.Row="1"
                               TextWrapping="Wrap"
                               Text="{Binding Text}">
                    </TextBlock>
                </Grid>
            </Grid>
        </DataTemplate>
        
        <DataTemplate x:Key="ChampionScore">
            <StackPanel Margin="0,0,10,5">
                <Image Source="Assets/upvoteblank.png"
                       Name="UpvoteImage"
                       Tapped="CounterUpvote_Tapped"
                       DataContextChanged="CounterUpvote_DataLoaded"
                       Height="25"
                       Width="25">
                </Image>
                <TextBlock Text="{Binding Score}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           FontSize="20">
                </TextBlock>
                <Image Source="Assets/downvoteblank.png"
                       Name="DownvoteImage"
                       Tapped="CounterDownvote_Tapped"
                       DataContextChanged="CounterDownvote_DataLoaded"
                       Height="25"
                       Width="25">
                </Image>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>



    <Grid x:Name="LayoutRoot"
          Background="{StaticResource BaronImageBrush}"
          KeyDown="Key_Down">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup >
                <VisualState x:Name="desktop">
                    <VisualState.StateTriggers>
                        <triggers:DeviceFamilyStateTrigger DeviceFamily="Desktop"/>
                        <triggers:DeviceFamilyStateTrigger DeviceFamily="Team"/>
                        <triggers:DeviceFamilyStateTrigger DeviceFamily="Unknown"/>
                        <triggers:DeviceFamilyStateTrigger DeviceFamily="Xbox"/>
                        <triggers:DeviceFamilyStateTrigger DeviceFamily="IoT"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="CounterSection.Width" Value="450" />
                        <Setter Target="CounterSection.Margin" Value="30,0,0,150" />
                        <Setter Target="EasyMatchupSection.Width" Value="450" />
                        <Setter Target="EasyMatchupSection.Margin" Value="30,0,0,150" />
                        <Setter Target="SynergySection.Width" Value="450" />
                        <Setter Target="SynergySection.Margin" Value="30,0,0,100" />
                        <Setter Target="CounterCommentSection.Width" Value="550" />
                        <Setter Target="CounterCommentSection.Margin" Value="30,0,0,150" />
                        <Setter Target="PlayingCommentSection.Width" Value="550" />
                        <Setter Target="PlayingCommentSection.Margin" Value="30,0,0,150" />
                        <Setter Target="SubmitFeedbackSection.Width" Value="600" />
                        <Setter Target="SubmitFeedbackSection.Margin" Value="30,0,0,150" />
                        <Setter Target="SubmitCounterSection.Width" Value="600" />
                        <Setter Target="SubmitCounterSection.Margin" Value="30,0,0,150" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="mobile">
                    <VisualState.StateTriggers>
                        <triggers:DeviceFamilyStateTrigger DeviceFamily="Mobile" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid>
            <Image Source="{Binding Champion.ImagePath}" VerticalAlignment="Center" HorizontalAlignment="Left" Width="70" Height="70"/>
            <TextBlock Text="{Binding Champion.UniqueId}"
                       Style="{ThemeResource HeaderTextBlockStyle}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontWeight="Bold"
                       FontSize="25" />
            <TextBlock Style="{ThemeResource HeaderTextBlockStyle}"
                       Margin="0,0,10,0"
                       FontWeight="Bold"
                       FontSize="25"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Text="{Binding Role}" />
        </Grid>

        <Grid Grid.Row="1" Loaded="GridAd_Loaded">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="300"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <Grid Grid.Column="1" Margin="250,0,0,0" x:Name="AdGrid">
                <Universal:AdMediatorControl x:Name="AdMediatorC1" Id="AdMediator-Id-WC1" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC2" Id="AdMediator-Id-WC2" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC3" Id="AdMediator-Id-WC3" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC4" Id="AdMediator-Id-WC4" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC5" Id="AdMediator-Id-WC5" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC6" Id="AdMediator-Id-WC6" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC7" Id="AdMediator-Id-WC7" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC8" Id="AdMediator-Id-WC8" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC9" Id="AdMediator-Id-WC9" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC10" Id="AdMediator-Id-WC10" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC11" Id="AdMediator-Id-WC11" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC12" Id="AdMediator-Id-WC12" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC13" Id="AdMediator-Id-WC13" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC14" Id="AdMediator-Id-WC14" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC15" Id="AdMediator-Id-WC15" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC17" Id="AdMediator-Id-WC17" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC18" Id="AdMediator-Id-WC18" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC19" Id="AdMediator-Id-WC19" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC20" Id="AdMediator-Id-WC20" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
                <Universal:AdMediatorControl x:Name="AdMediatorC21" Id="AdMediator-Id-WC21" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
            </Grid>
        <Grid Grid.Column="1" Name="AdGrid2">
                <Universal:AdMediatorControl x:Name="AdMediatorC16" Id="AdMediator-Id-WC16" Style="{StaticResource HorizontalAdMediator}" Loaded="Ad_Loaded"/>
            </Grid>
        
        <Hub Name="MainHub"
             Margin="0,-15,0,0">
            <HubSection x:Name="CounterSection" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                    <DataTemplate>
                        <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                            
                        <TextBlock FontSize="30"
                                       Margin="0,-10,0,0"
                                       HorizontalAlignment="Center"
                                       Style="{StaticResource TitleTextBlockStyle}"
                                       Text="Counters">
                                </TextBlock>

                            <Grid Grid.Row="1">
                                <ProgressRing VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          HorizontalContentAlignment="Center"
                                          VerticalContentAlignment="Center"
                                          Height="150"
                                          Width="150"
                                          Loaded="CounterRing_Loaded"
                                          IsActive="True">

                                </ProgressRing>

                            <ListView ItemsSource="{Binding ChampionFeedback.Counters, Converter={StaticResource CounterChampioniVisibility}}"
                                      IsItemClickEnabled="True"
                                      IsSwipeEnabled="False"
                                      SelectionMode="None">
                                <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment"
                                                Value="Stretch" />
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                    <ColumnDefinition Width="1.1*"></ColumnDefinition>
                                                    <ColumnDefinition Width="4*"></ColumnDefinition>
                                                </Grid.ColumnDefinitions>

                                                <ContentControl ContentTemplate="{StaticResource ChampionScore}"></ContentControl>

                                                <Image Grid.Column="1"
                                                   Tapped="Champ_Tapped"
                                                   Source="{Binding Name, Converter={StaticResource NameToImageConverter}}">
                                                </Image>

                                                <ProgressBar Grid.Column="2"
                                                         Height="12"
                                                         Foreground="Crimson"
                                                         Margin="15,0,25,0"
                                                         Value="{Binding Value}">
                                                </ProgressBar>

                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </HubSection>

            <HubSection x:Name="EasyMatchupSection" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"> 
                    <DataTemplate>
                        <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock FontSize="30"
                                       Margin="0,-10,0,0"
                                       HorizontalAlignment="Center"
                                       Style="{StaticResource TitleTextBlockStyle}"
                                       Text="Easy Matchups">
                        </TextBlock>

                        <Grid Grid.Row="1">
                            <ProgressRing VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          Height="150"
                                          Width="150"
                                          Loaded="EasyMatchupRing_Loaded"
                                          IsActive="True">

                            </ProgressRing>

                            <ListView ItemsSource="{Binding ChampionFeedback.EasyMatchups}"
                                      IsItemClickEnabled="True"
                                      IsSwipeEnabled="False"
                                      SelectionMode="None">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment"
                                                Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                <ColumnDefinition Width="1.1*"></ColumnDefinition>
                                                <ColumnDefinition Width="4*"></ColumnDefinition>
                                            </Grid.ColumnDefinitions>

                                            <ContentControl ContentTemplate="{StaticResource ChampionScore}"></ContentControl>

                                            <Image Grid.Column="1"
                                                   Tapped="EasyMatchup_Tapped"
                                                   Source="{Binding ChampionFeedbackName, Converter={StaticResource NameToImageConverter}}">
                                            </Image>

                                            <ProgressBar Grid.Column="2"
                                                         Height="12"
                                                         Foreground="Green"
                                                         Margin="15,0,25,0"
                                                         Value="{Binding Value}">
                                            </ProgressBar>

                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </HubSection>

            <HubSection x:Name="SynergySection" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                    <DataTemplate>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock FontSize="30"
                                       Margin="0,-10,0,0"
                                       HorizontalAlignment="Center"
                                       Style="{StaticResource TitleTextBlockStyle}"
                                       Text="Synergy">
                        </TextBlock>

                        <Grid Grid.Row="1">
                                <ProgressRing VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          Height="150"
                                          Width="150"
                                          Loaded="SynergyRing_Loaded"
                                          IsActive="True">

                                </ProgressRing>

                                <TextBlock
                                       Visibility="Collapsed"
                                       FontSize="30"
                                       TextAlignment="Center"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Loaded="SynergyChampions_Loaded"
                                       TextWrapping="Wrap"> Be the first to submit a synergy champion!
                                </TextBlock>


                                <ListView ItemsSource="{Binding ChampionFeedback.Counters, Converter={StaticResource SynergyChampioniVisibility}}"
                                      IsItemClickEnabled="True"
                                      IsSwipeEnabled="False"
                                      SelectionMode="None">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="HorizontalContentAlignment"
                                                Value="Stretch" />
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                    <ColumnDefinition Width="1.1*"></ColumnDefinition>
                                                    <ColumnDefinition Width="4*"></ColumnDefinition>
                                                </Grid.ColumnDefinitions>

                                                <ContentControl ContentTemplate="{StaticResource ChampionScore}"></ContentControl>

                                                <Image Grid.Column="1"
                                                   Tapped="SynergyChamp_Tapped"
                                                   DataContextChanged="Synergy_Loaded">
                                                </Image>

                                                <ProgressBar Grid.Column="2"
                                                         Height="12"
                                                         Foreground="#FF771287"
                                                         Margin="15,0,25,0"
                                                         Value="{Binding Value}">
                                                </ProgressBar>

                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </HubSection>


            <HubSection x:Name="CounterCommentSection" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                    <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>

                        <TextBlock FontSize="30"
                                       Margin="0,-10,0,0"
                                       Style="{StaticResource TitleTextBlockStyle}"
                                       Text="{Binding Champion.UniqueId, Converter={StaticResource CounterConverter}}"
                                       VerticalAlignment="Center"
                                       TextAlignment="Center">
                        </TextBlock>


                        <Grid Grid.Row="1">
                            <ProgressRing 
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      Height="150"
                                      Width="150"
                                      Loaded="CounterCommentsRing_Loaded"
                                      IsActive="True">
                            </ProgressRing>

                            <TextBlock 
                                   Visibility="Collapsed"
                                   Name="NoCommentMessage"
                                   FontSize="30"
                                   TextAlignment="Center"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Loaded="CounterMessage_Loaded"
                                   TextWrapping="Wrap"> Be the first to submit a comment!</TextBlock>

                            <ListView ItemsSource="{Binding ChampionFeedback.Comments, Converter={StaticResource CounterVisibilityConverter}}"
                                      IsItemClickEnabled="True"
                                      SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl ContentTemplate="{StaticResource CommentSection}" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </HubSection>

            <HubSection x:Name="PlayingCommentSection" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>

                        <TextBlock FontSize="30"
                                       Margin="0,-10,0,0"
                                       Style="{StaticResource TitleTextBlockStyle}"
                                       Text="{Binding Champion.UniqueId, Converter={StaticResource PlayingConverter}}"
                                       VerticalAlignment="Top"
                                       HorizontalAlignment="Center"
                                       TextAlignment="Center">
                        </TextBlock>

                        <Grid Grid.Row="1">
                            <ProgressRing 
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      Height="150"
                                      Width="150"
                                      Loaded="PlayingCommentsRing_Loaded"
                                      IsActive="True">
                            </ProgressRing>

                            <TextBlock 
                                   Visibility="Collapsed"
                                   Name="NoCommentMessage2"
                                   FontSize="30"
                                   TextAlignment="Center"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Loaded="PlayingComments_Loaded"
                                   TextWrapping="Wrap"> Be the first to submit a comment!
                            </TextBlock>

                            <ListView ItemsSource="{Binding ChampionFeedback.Comments, Converter={StaticResource PlayingVisibilityConverter}}"
                                      IsItemClickEnabled="True"
                                      SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl ContentTemplate="{StaticResource CommentSection}" />
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </HubSection>


            <HubSection x:Name="SubmitFeedbackSection" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" VerticalAlignment="Stretch">
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                       
                        <TextBlock x:Name="Feedback"
                                   FontSize="30"
                                   Margin="0,-10,0,0"
                                   Style="{StaticResource TitleTextBlockStyle}"
                                   Text="Submit Comments?"
                                   VerticalAlignment="Top"
                                   HorizontalAlignment="Center"
                                   TextAlignment="Center">
                        </TextBlock>


                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>

                            <RadioButton GroupName="Comment"
                                         HorizontalAlignment="Right"
                                         Checked="Counter_Checked"
                                         Content="Counter">
                            </RadioButton>
                            <RadioButton GroupName="Comment"
                                         Grid.Column="1"
                                         HorizontalAlignment="Left"
                                         Checked="Playing_Checked"
                                         Content="Playing as">
                            </RadioButton>
                        </Grid>

                        <TextBox Margin="30,0,30,0"
                                 Grid.Row="2"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"
                                 Background="White"
                                 Text="Your name"
                                 TextAlignment="Center"
                                 GotFocus="Name_Focus"
                                 LostFocus="Name_Written"
                                 Loaded="NameBox_Loaded"></TextBox>

                        <TextBox HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"
                                 Background="White"
                                 Margin="0,10,0,10"
                                 Grid.Row="3"
                                 Loaded="FeedbackBox_Loaded"
                                 TextWrapping="Wrap"></TextBox>

                        <Button Grid.Row="4"
                                VerticalAlignment="Bottom"
                                HorizontalAlignment="Center"
                                Tapped="Send_Feedback">Send</Button>

                    </Grid>
                </DataTemplate>
            </HubSection>

            <HubSection x:Name="SubmitCounterSection" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch" >
                <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>

                            <TextBlock x:Name="Feedback"
                                   FontSize="30"
                                   Margin="0,-10,0,0"
                                   Style="{StaticResource TitleTextBlockStyle}"
                                   Text="Submit Champion?"
                                   VerticalAlignment="Top"
                                   TextAlignment="Center">
                            </TextBlock>


                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <RadioButton GroupName="Champion"
                                     MinWidth="50"
                                     HorizontalAlignment="Right"
                                     Checked="CounterChampion_Checked"
                                     Content="Counter"></RadioButton>
                                <RadioButton GroupName="Champion"
                                     Grid.Column="1"
                                     MinWidth="50"
                                     HorizontalAlignment="Center"
                                     Checked="EasyMatchupChampion_Checked"
                                     Content="Easy"></RadioButton>
                                <RadioButton GroupName="Champion"
                                     Grid.Column="2"    
                                     MinWidth="50"
                                     HorizontalAlignment="Left"
                                     Checked="SynergyChampion_Checked"
                                     Content="Synergy"></RadioButton>
                            </Grid>

                            <TextBox Height="40"
                                 Name="FilterBox"
                                 Margin="30,0,30,0"
                                 Loaded="FilterBox_Loaded"
                                 Grid.Row="2"
                                 Background="White"
                                 GotFocus="Filter_GotFocus"
                                 TextAlignment="Center"
                                 Text="Type to search"
                                 TextChanged="Filter_TextChanged">
                            </TextBox >

                            <GridView ItemsSource="{Binding Filter}"
                                  IsItemClickEnabled="True"
                                  HorizontalAlignment="Center"
                                  Grid.Row="3"
                                  Margin="0,10,0,10"
                                  ItemClick="Counter_Click">
                                <GridView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Image Source="{Binding ImagePath}"
                                               Width="50"
                                               Height="50" />
                                        </Grid>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>

                            <Button Grid.Row="4"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Tapped="Submit_Counter"
                                Content="Submit">
                            </Button>
                        </Grid>
                    </DataTemplate>
                </HubSection>
            </Hub>
        </Grid>
    </Grid>
</Page>